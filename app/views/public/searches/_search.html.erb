<% if user_signed_in? %>
  <div class="search_form">
    <%= form_with url: search_path, method: :get do |f| %>
      <%= f.text_field :word %>
      <%= f.submit "検索", class: 'btn btn-sm' %>
    <% end %>
  </div>
<% end %>
